import styled from 'styled-components';

export const TagsContainer = styled.ul`
    display: flex;
    align-items: center;
    height: fit-content;

    padding: 10px 0px;
    box-sizing: border-box;
`