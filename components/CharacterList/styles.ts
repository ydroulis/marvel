import styled from 'styled-components';

export const ListContainer = styled.ul`
    width: 100%;

    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;

    position: absolute;
    left: 0px;
    top: 100px;
`